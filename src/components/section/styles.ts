import { styled } from '~/stitches.config';

export const Container = styled('section', {
  paddingBottom: '1rem',
});

export const Title = styled('h1', {
  marginBottom: '2.5rem',

  fontSize: '1.25rem',
  fontWeight: 600,
});
