import { styled } from '~/stitches.config';

export const Container = styled('footer', {
  marginTop: 'auto',
  py: '1rem',

  fontSize: '0.875rem',
  textAlign: 'center',
  color: '$textTertiary',
});
