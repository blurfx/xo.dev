import styled from 'styled-components';

export const Button = styled.button`
  margin-left: 0.5em;
  padding: 0;
  border: 0;

  font-size: 1.275em;

  background: transparent;
  outline: none;
  cursor: pointer;
  
  appearance: none;
`;
